import { useHistory } from 'react-router-dom';

export default function CreateActor() {
  const history = useHistory();
  return (
    <>
      <h3>Create Actor</h3>
      Save Changes
    </>
  );
}
